{
  "name": "pipeline-guard",
  "version": "0.6.1",
  "private": true,
  "description": "Browser-based local-first DevSecOps repository preflight scanner",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
